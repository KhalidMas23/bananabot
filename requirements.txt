# Runtime (vision, overlay, camera)
# pytesseract also needs the Tesseract OCR engine installed on the system (PATH or config).
opencv-python>=4.8.0
numpy>=1.24.0
pytesseract>=0.3.10

# Tests
pytest>=7.0.0
